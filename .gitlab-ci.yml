stages:
  - test
  - build

test project:
  image: node:15
  script:
    - yarn install
    - yarn test

build project:
  image: node:15
  script:
    - yarn install
    - yarn build
  artifacts:
    paths:
      - build/


  
